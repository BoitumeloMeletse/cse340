
<!-- views/partials/header.ejs -->
<header id="top-header">
  <span class="siteName">
    <a href="/" title="Return to home page">CSE Motors</a>
  </span>

  <div id="tools">
    <% if (locals.loggedin && locals.accountData) { %>
      <!-- Visible only when logged in: Welcome (link to /account/) then Logout -->
      <a title="Click to access account management" href="/account/">Welcome <%= locals.accountData.account_firstname %></a>
      <a title="Click to log out" href="/account/logout">Logout</a>
    <% } else { %>
      <!-- Visible only when not logged in -->
      <a title="Click to log in" href="/account/login">My Account</a>
    <% } %>
  </div>
</header>

